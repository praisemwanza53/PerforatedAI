program: train.py
method: grid
metric:
  name: accuracy
  goal: maximize
command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args}
parameters:
  use_dendritic:
    values: [0, 1]
  learning_rate:
    values: [0.001]
  batch_size:
    values: [1024]